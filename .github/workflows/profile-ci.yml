name: Profile CI

on:
  push:
    branches:
      - master
  schedule:
    - cron: '0 3 */4 * *'
  workflow_dispatch:

# Least privilege by default; the single job opts in to contents: write.
permissions: {}

# Never let a scheduled run and a push run push to master at the same time.
concurrency:
  group: profile-ci-${{ github.ref }}
  cancel-in-progress: false

jobs:
  update-profile:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Check out the branch tip rather than the commit that triggered the run.
          # Several pushes landing close together (merging a batch of Dependabot PRs,
          # for instance) queue behind the concurrency group, and a run started from
          # a stale commit cannot fast-forward when it finally pushes.
          ref: ${{ github.event.repository.default_branch }}

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        # Pinned: an unpinned install changes under the pipeline without a commit.
        run: pip install --disable-pip-version-check requests==2.34.2 pyyaml==6.0.3

      - name: Count commits across own repositories
        env:
          # GITHUB_TOKEN is minted per run and never expires — no manual rotation.
          # GH_PAT is optional and only needed to also count private repositories.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          python3 - << 'EOF'
          import json
          import os
          import sys
          from datetime import date

          import requests

          pat = os.environ.get("GH_PAT", "").strip()
          token = pat or os.environ["GITHUB_TOKEN"]

          USER = "geminishkv"
          # Derived from the clock so the badges roll over on 1 January by themselves.
          CURRENT_YEAR = date.today().year
          YEARS = (CURRENT_YEAR - 1, CURRENT_YEAR)

          # Only a user-scoped PAT can enumerate private repositories; the built-in
          # GITHUB_TOKEN has no user context, so fall back to the public listing.
          REPOS_URL = (
              "https://api.github.com/user/repos?per_page=100&type=owner"
              if pat
              else f"https://api.github.com/users/{USER}/repos?per_page=100&type=owner"
          )
          print(f"Auth: {'GH_PAT' if pat else 'GITHUB_TOKEN (public repositories only)'}")

          session = requests.Session()
          session.headers.update({
              "Authorization": f"Bearer {token}",
              "Accept": "application/vnd.github+json",
              "X-GitHub-Api-Version": "2022-11-28",
          })

          def get(url, **kwargs):
              resp = session.get(url, timeout=30, **kwargs)
              if resp.status_code == 401:
                  sys.exit(
                      "Token rejected (401). If GH_PAT is set it is invalid, revoked or "
                      "expired — delete the secret to fall back to GITHUB_TOKEN."
                  )
              if resp.status_code == 403 and resp.headers.get("X-RateLimit-Remaining") == "0":
                  sys.exit("GitHub API rate limit exhausted.")
              return resp

          def get_repos():
              """Own, non-fork repositories — forks would inflate the numbers."""
              repos = []
              url = REPOS_URL
              while url:
                  resp = get(url)
                  resp.raise_for_status()
                  repos.extend(r["name"] for r in resp.json() if not r["fork"])
                  url = resp.links.get("next", {}).get("url")
              return repos

          def count_commits(repo, year):
              """Commits authored by USER in `repo` during `year`."""
              params = {
                  "author": USER,
                  "since": f"{year}-01-01T00:00:00Z",
                  "until": f"{year}-12-31T23:59:59Z",
                  "per_page": 100,
              }
              url = f"https://api.github.com/repos/{USER}/{repo}/commits"
              total = 0
              while url:
                  resp = get(url, params=params)
                  if resp.status_code in (404, 409):  # missing or empty repository
                      return 0
                  resp.raise_for_status()
                  total += len(resp.json())
                  url = resp.links.get("next", {}).get("url")
                  params = None  # the pagination link already carries the query
              return total

          repos = get_repos()
          data = {f"commits_{year}": 0 for year in YEARS}
          for repo in repos:
              for year in YEARS:
                  data[f"commits_{year}"] += count_commits(repo, year)
          data["updated"] = date.today().isoformat()

          with open("commits.json", "w", encoding="utf-8") as f:
              json.dump(data, f, indent=2)
              f.write("\n")

          print(f"Repos: {len(repos)} | " + " | ".join(f"{y}: {data[f'commits_{y}']}" for y in YEARS))
          EOF

      - name: Pull profile data from geminishkv.tech sources
        env:
          SITE_REPO: geminishkv/gpages_intro
          SITE_REF: gpages
        run: |
          set -euo pipefail
          base="https://raw.githubusercontent.com/${SITE_REPO}/${SITE_REF}"
          curl -fsSL "${base}/src/i18n/translations.js"    -o "${RUNNER_TEMP}/translations.js"
          curl -fsSL "${base}/src/components/Tools.js"     -o "${RUNNER_TEMP}/Tools.js"

          cat > "${RUNNER_TEMP}/extract.js" << 'EOF'
          // Reads the site's own source of truth so the profile can never drift from
          // geminishkv.tech. Both inputs come from a repository owned by the same user.
          const fs = require('fs');
          const vm = require('vm');

          function sliceLiteral(src, decl) {
            const start = src.indexOf(decl);
            if (start === -1) throw new Error(`declaration not found: ${decl}`);
            const open = src.indexOf('[', start);
            let depth = 0;
            for (let i = open; i < src.length; i++) {
              const c = src[i];
              if (c === '[') depth++;
              else if (c === ']' && --depth === 0) return src.slice(open, i + 1);
            }
            throw new Error(`unbalanced literal: ${decl}`);
          }

          const trSrc = fs.readFileSync(process.argv[2], 'utf8')
            .replace('export const translations', 'const translations');
          const ctx = { process: { env: { PUBLIC_URL: '' } }, module: { exports: {} } };
          vm.createContext(ctx);
          vm.runInContext(trSrc + '\nmodule.exports = translations;', ctx);
          const en = ctx.module.exports.en;

          const toolsSrc = fs.readFileSync(process.argv[3], 'utf8');
          const toolCtx = vm.createContext({});
          const TOOL_GROUPS = vm.runInContext(sliceLiteral(toolsSrc, 'const TOOL_GROUPS'), toolCtx);

          const out = {
            experience: en.experience.map((e) => ({
              period: e.period,
              role: e.role,
              companies: e.logos
                ? e.logos.map((l) => ({ name: l.alt, url: l.url }))
                : [{ name: e.company, url: e.url }],
            })),
            lead: en.hero.leadItems,
            now: en.now.items.map((i) => ({ text: i.text, href: i.href })),
            projects: en.projects.map((p) => ({
              name: p.name, desc: p.desc, lang: p.lang, url: p.url,
            })),
            toolGroups: TOOL_GROUPS.map((g) => ({
              label: g.label, items: g.items.map((i) => i[0]),
            })),
          };

          for (const [key, value] of Object.entries(out)) {
            if (!Array.isArray(value) || value.length === 0) {
              throw new Error(`extracted "${key}" is empty — the site source layout changed`);
            }
          }
          console.log(JSON.stringify(out, null, 2));
          EOF

          node "${RUNNER_TEMP}/extract.js" \
            "${RUNNER_TEMP}/translations.js" \
            "${RUNNER_TEMP}/Tools.js" > "${RUNNER_TEMP}/site-data.json"

          python3 -c "import json,os;d=json.load(open(os.environ['RUNNER_TEMP']+'/site-data.json'));print('Synced: %d roles, %d projects, %d tool groups, %d now items'%(len(d['experience']),len(d['projects']),len(d['toolGroups']),len(d['now'])))"

      - name: Render stats cards to committed SVGs
        env:
          # Own github-readme-stats instance. It is contacted once per run here
          # instead of on every profile view, and the rendered SVGs are committed,
          # so the profile keeps working even when the instance is down.
          CARDS_BASE: https://github-readme-stats-tau-one-20.vercel.app
          # Brand palette: black ground, gold titles, red icons.
          BRAND: hide_border=true&bg_color=0a0a0a&title_color=d4a520&text_color=e6edf3&icon_color=cc2200&ring_color=d4a520
        run: |
          set -euo pipefail
          mkdir -p artifacts/cards

          fetch_card() {
            local url="$1" dest="$2" tmp="${RUNNER_TEMP}/card.svg"
            if curl -fsS --max-time 60 "$url" -o "$tmp" \
               && [ -s "$tmp" ] && grep -qi '<svg' "$tmp"; then
              mv "$tmp" "$dest"
              echo "updated $dest ($(wc -c < "$dest") bytes)"
            else
              # Keeping the committed copy is the whole point of rendering ahead
              # of time — a bad fetch must never blank the profile.
              echo "::warning::could not refresh ${dest}, keeping the committed copy"
            fi
          }

          # The two cards are sized to land within ~3% of each other (270 vs 279 px),
          # so the readme can pin both to one height without visible rescaling.
          fetch_card "${CARDS_BASE}/api?username=geminishkv&show_icons=true&count_private=true&include_all_commits=true&custom_title=AppSec%20%26%20DevSecOps%20Stats&show=reviews,prs_merged,prs_merged_percentage&${BRAND}" \
            artifacts/cards/stats.svg

          fetch_card "${CARDS_BASE}/api/top-langs/?username=geminishkv&layout=donut&langs_count=8&exclude_repo=DevSecOps_wiki&${BRAND}" \
            artifacts/cards/top-langs.svg

      - name: Collect Scorecard results
        run: |
          python3 - << 'EOF'
          import json
          import os
          import pathlib

          import requests

          USER = "geminishkv"
          SCORECARD = "https://api.scorecard.dev/projects/github.com/{}/{}"

          site = json.loads(
              pathlib.Path(os.environ["RUNNER_TEMP"], "site-data.json").read_text("utf-8")
          )

          def score(repo):
              """The project's OpenSSF score, or None until its Scorecard workflow
              has published a first run."""
              try:
                  resp = requests.get(SCORECARD.format(USER, repo), timeout=30)
              except requests.RequestException:
                  return None
              if resp.status_code != 200:
                  return None
              return resp.json().get("score")

          repos = {p["name"]: score(p["name"]) for p in site["projects"]}
          pathlib.Path(os.environ["RUNNER_TEMP"], "posture.json").write_text(
              json.dumps({"repos": repos}, indent=2), encoding="utf-8"
          )
          scored = sum(1 for v in repos.values() if v is not None)
          print(f"Scorecard: {scored}/{len(repos)} repositories scored")
          EOF

      - name: Render generated blocks in readme
        run: |
          python3 - << 'EOF'
          import json
          import os
          import pathlib
          import re

          import yaml

          COMMITS_JSON = (
              "https://raw.githubusercontent.com/geminishkv/geminishkv/master/commits.json"
          )

          site = json.loads(
              pathlib.Path(os.environ["RUNNER_TEMP"], "site-data.json").read_text("utf-8")
          )
          posture = json.loads(
              pathlib.Path(os.environ["RUNNER_TEMP"], "posture.json").read_text("utf-8")
          )
          commits = json.loads(pathlib.Path("commits.json").read_text("utf-8"))

          def cell(text):
              return str(text).replace("|", "\\|")

          def link(name, url):
              return f"[{cell(name)}]({url})" if url else cell(name)

          def table(header, rows):
              head = "| " + " | ".join(header) + " |"
              sep = "| " + " | ".join("---" for _ in header) + " |"
              return "\n".join([head, sep, *rows])

          experience = table(
              ["Period", "Company", "Role"],
              [
                  "| {} | {} | {} |".format(
                      cell(e["period"]),
                      " / ".join(link(c["name"], c.get("url")) for c in e["companies"]),
                      cell(e["role"]),
                  )
                  for e in site["experience"]
              ],
          )

          def scorecard_cell(repo):
              """Links the score to its full OpenSSF report; an em dash until the
              Scorecard workflow has published a first run for that repository."""
              score = posture["repos"].get(repo)
              if score is None:
                  return "—"
              url = f"https://scorecard.dev/viewer/?uri=github.com/geminishkv/{repo}"
              return f"[{score:.1f}]({url})"

          projects = table(
              ["Project", "Description", "Stack", "Scorecard"],
              [
                  "| {} | {} | {} | {} |".format(
                      link(p["name"], p["url"]),
                      cell(p["desc"]),
                      cell(p["lang"]),
                      scorecard_cell(p["name"]),
                  )
                  for p in site["projects"]
              ],
          )

          toolchain = table(
              ["Area", "Tools"],
              [
                  "| {} | {} |".format(cell(g["label"]), " · ".join(map(cell, g["items"])))
                  for g in site["toolGroups"]
              ],
          )

          # The certificates table used to come from a third-party action that runs
          # on Node 20 and relies on the deprecated set-output command. It is a plain
          # table, so it is built here with the same helpers as the other blocks.
          CERT_COLUMNS = ["ContentId", "Area", "PageTitle", "MetaDescription"]
          certs = yaml.safe_load(pathlib.Path("certs.yaml").read_text("utf-8")) or []
          if not certs:
              raise SystemExit("certs.yaml has no entries — the certificates table would be empty")
          for number, cert in enumerate(certs, 1):
              absent = [column for column in CERT_COLUMNS if not cert.get(column)]
              if absent:
                  raise SystemExit(f"certs.yaml entry {number} lacks {', '.join(absent)}")
          certs_table = table(
              CERT_COLUMNS,
              ["| " + " | ".join(cell(cert[column]) for column in CERT_COLUMNS) + " |" for cert in certs],
          )

          def wrap(text, width=24):
              """Mermaid re-wraps long label lines on its own and inflates the shape,
              so lines are broken here."""
              lines, current = [], ""
              for word in str(text).replace('"', "'").split():
                  if current and len(current) + 1 + len(word) > width:
                      lines.append(current)
                      current = word
                  else:
                      current = f"{current} {word}".strip()
              return "<br/>".join([*lines, current])

          def years(period):
              """'Apr 2019 — Jan 2020' -> '2019–2020', an open period ends with 'now'."""
              found = re.findall(r"\d{4}", period)
              if not found:
                  return period
              end = "now" if re.search(r"present|now", period, re.IGNORECASE) else found[-1]
              return found[0] if end == found[0] else f"{found[0]}–{end}"

          def experience_flow(roles):
              """The career pipeline of geminishkv.tech: first role on the left, the
              current one closes the line on the right. Left to right on purpose, it
              is a conveyor like the ones on the sites; labels are kept narrow because
              GitHub scales a wide diagram down to the readme width."""
              ordered = list(reversed(roles))
              nodes, edges = [], []
              for number, role in enumerate(ordered, 1):
                  label = "<br/>".join(
                      [
                          wrap(" / ".join(company["name"] for company in role["companies"]), 14),
                          wrap(role["role"], 14),
                          years(role["period"]),
                      ]
                  )
                  last = number == len(ordered)
                  shape = '(["{}"])' if last else '["{}"]'
                  nodes.append(f"    role_{number}" + shape.format(label))
                  if number > 1:
                      edges.append(f"    role_{number - 1} --> role_{number}")
              past = ",".join(f"role_{n}" for n in range(1, len(ordered)))
              return "\n".join(
                  [
                      "```mermaid",
                      '%%{init: {"flowchart": {"curve": "step"}}}%%',
                      "flowchart LR",
                      "    accTitle: Career pipeline",
                      "    accDescr: Roles from the first one on the left to the current one on the right, the same pipeline as on geminishkv.tech.",
                      "",
                      *nodes,
                      "",
                      *edges,
                      "",
                      "    classDef stage fill:#dbeafe,stroke:#2563eb,stroke-width:2px,color:#1e3a5f",
                      "    classDef done fill:#dcfce7,stroke:#16a34a,stroke-width:2px,color:#14532d",
                      "",
                      f"    class {past} stage",
                      f"    class role_{len(ordered)} done",
                      "```",
                  ]
              )

          flow = experience_flow(site["experience"])

          intro = "\n".join(f"* {item}" for item in site["lead"])

          now = "\n".join(f"* [{cell(item['text'])}]({item['href']})" for item in site["now"])

          # Badge labels carry the year, so they are regenerated from commits.json
          # instead of being hardcoded — nothing to edit every January.
          years = sorted(
              int(k.rsplit("_", 1)[1]) for k in commits if re.fullmatch(r"commits_\d{4}", k)
          )
          badges = "\n".join(
              f"![Commits {y}](https://img.shields.io/badge/dynamic/json"
              f"?url={COMMITS_JSON}&label=Commits%20{y}&query=commits_{y}"
              f"&color=2448a2&cacheSeconds=300)"
              for y in years
          )

          INLINE = {"COMMIT-BADGES"}

          blocks = {
              "COMMIT-BADGES": badges,
              "INTRO": intro,
              "NOW": now,
              "EXPERIENCE-FLOW": flow,
              "EXPERIENCE": experience,
              "PROJECTS": projects,
              "TOOLCHAIN": toolchain,
              "CERTS": f"\n***\n\n### Certificates\n\n{certs_table}\n",
          }

          readme = pathlib.Path("readme.md")
          content = readme.read_text(encoding="utf-8")

          rendered, skipped = [], []
          for name, body in blocks.items():
              start, end = f"<!-- {name}-START -->", f"<!-- {name}-END -->"
              pattern = re.compile(
                  re.escape(start) + r".*?" + re.escape(end), re.DOTALL
              )
              if not pattern.search(content):
                  # The readme is hand-edited too. A dropped marker pair means the
                  # owner took that block over — say so loudly, but never fail the
                  # run and never re-insert content they removed on purpose.
                  print(f"::warning::no {name} marker pair in readme.md, block skipped")
                  skipped.append(name)
                  continue
              if name in INLINE:
                  # A marker on a line of its own starts an HTML block and splits the
                  # paragraph, so the badge row breaks into three. Here the opening
                  # marker ends the previous line and the closing one ends the last.
                  replacement = f"{start}\n{body} {end}"
              else:
                  replacement = f"{start}\n{body}\n{end}"
              content = pattern.sub(lambda _: replacement, content, count=1)
              rendered.append(name)

          readme.write_text(content, encoding="utf-8")
          print("Rendered blocks: " + (", ".join(rendered) or "none"))
          if skipped:
              print("Skipped blocks: " + ", ".join(skipped))
          EOF

      - name: Rebase onto anything pushed while the job was running
        run: git pull --rebase --autostash origin "${GITHUB_REF_NAME}"

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@4a55954c782fc1ea30b9056cd3e7a2b40ca8887d # v7.2.0
        with:
          commit_message: "chore: refresh commit stats and certificates (${{ github.ref_name }})"
          file_pattern: 'commits.json readme.md artifacts/cards/*.svg'
